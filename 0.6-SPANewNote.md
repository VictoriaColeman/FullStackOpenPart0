## New Note in Single Page App Diagram
```mermaid
sequenceDiagram
    note right of Browser: user submits new note via form
    Browser->>Server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    note right of Browser: event handler from JavaScript code causes new note to be appended to notes list<br/>HTML document is updated with new notes list and is rendered to the page<br/>data for new note is sent to server in JSON format
```
